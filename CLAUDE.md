# バイトのホンネ

## これは何か

働く側の声で「このバイト、自分に合うか」を判断できるレビューアプリ。求人票に載らない
**雰囲気・同世代の割合・シフトの融通・支払いサイクル**を、**投稿者の属性つき**で見られる。

---

## このプロジェクトの制約

**この制約は変更しないでください。**

- **素の HTML / CSS / JavaScript のみ。** ビルドツール・パッケージマネージャ・フレームワークは使わない
- **データは localStorage に保存する。** サーバーやデータベースは作らない
- **`index.html` をブラウザで開けば動くこと。** 開発サーバーを立てる必要がない状態を保つ
- **外部ライブラリは CDN から読み込まない。** 会場のネットワークが不安定なため、オフラインでも動く状態にする
- **主要な動線を1本だけ通す。** ログイン・管理画面・設定画面は作らない
- **最後の判断は人がする形にする。** 全自動にしない。AI が提案し、人が選ぶ／決める

---

## 申し送り（交代のたびに書き換える）

**9/4 の前半（〜16:15）は10分ごとに交代します。そのたびにここを書き換えてください。**
**後半（16:35〜）は交代しません。**
**開発期間（9/5〜9/24）は、次に作業する自分に宛てて書いてください。** 数日空くと忘れます。

- **今動いているもの**: `main` に全部入っている。スマホアプリ風の1カラム（幅430px）で、探す人の画面だけ。①重視チップ → ②写真サムネ＋業種絵文字のカード（加重平均で並び替え） → 開くとスコア・事実・レビュー → ③下に貼りつく「あなたの判断」シート
- **今やりかけのこと**: なし。`dev/wataru` と `polish/mobile-ui` は `main` に合流済み
- **次にやること**: **レビューの投稿画面**（項目定義 `TENURES` / `AGE_BANDS` / `ROLES` / `FACT_FIELDS` / `RATING_ITEMS` と保存処理は残してある）。GitHub Pages は有効化済み（`main` / root、URL: https://2026-inter-univ-hackathon.github.io/team-c/ ）。`main` に push すれば数十秒で反映される
- **ハマっていること**: `index.html` 1枚に全員が書くので、同時に触るとコンフリクトする。作業前に `git pull` すること

---

## 決めたこと（消さずに追記する）

チームで決めたことをここに足していく。ドライバーが替わっても設計がぶれないようにするため。

**14:45 の投稿の「今日は作らないもの」も、ここに書き写してください。** 開発中に蒸し返さないためです。

- 保存するデータは `localStorage` のキー `app-data` にまとめる（`workplaces` / `reviews` / `focus` / `decisions` の4つ）
- 画面は `index.html` の1枚だけにする
- 動線は1本：**①重視を選ぶ → ②並び替え → ③中身を見る → ④人が決める**（＋⑤投稿すると②に戻ってくる）
- **レビュー項目は3種類に分ける。** 全部を★5段階にすると入力が重くて誰も投稿しないため
  - **A 投稿者の属性**（信頼度の材料・3つだけ）: 勤続期間 / 年齢層 / 立場
  - **B 事実項目**（選択式・平均を取らずそのまま表示）: 支払いサイクル / 大学生の割合 / 募集状況 / 応募経路 / 実際の業務内容（自由記述）
  - **C 5段階評価**（6項目）: 話しかけやすさ / 周りの環境 / 労働時間の負担 / 職場内の繋がり / サポートの手厚さ / シフトの融通
- **平均点は単純平均ではなく加重平均にする。** 勤続2年以上=1.0 → 1年=0.85 → 半年=0.7 → 3か月未満=0.5、コメントありは +0.1。画面にもその旨を明記する
- レビュー1件ごとに「参考度 高／中／低」と属性バッジを出す。**「誰が言っているか」が見えることがこのアプリの核**
- 「労働時間の負担」は**点数が高いほど負担が軽い**（他の項目と向きを揃えるため）
- **画像は `img/` に同梱したものだけを使う。** 外部URLは参照しない（オフライン制約のため）。サムネイルは Unsplash の写真、アプリアイコンと未知カテゴリ用は自前 SVG。カテゴリ→写真は `CATEGORY_IMG`、業種の絵文字バッジは `CATEGORY_EMOJI`
- **（9/4 更新）サムネイルは実写真に変更。** Unsplash の写真を `img/*.jpg` としてリポジトリに**同梱**する（外部 URL は参照しないのでオフライン制約は満たす。Unsplash License で利用可・画面に「写真はイメージです」と明記）。アイコンとフォールバック（`img/other.svg`）は引き続き自前 SVG
- 画面はスマホ前提。`.app` を最大430pxの1カラムにして、PC で開いても同じ見た目にする（発表はスマホで見せる）
- デモ用のシードデータは JS 内の `seed()` に持つ。localStorage が空なら流し込む（GitHub Pages で初見の人の画面が空にならないようにするため）
- **今日は作らないもの**: ログイン／会員登録・バイト先の新規追加・検索・画像アップロード・通報・レビューの編集／削除・「役に立った」ボタン・地図・通知・サーバ同期

---

## AI に任せない部分

このハッカソンの共通制約です。**最後の判断を人が持つ場所**を1つ決めて、ここに書いてください。
発表で「AI に足りないのは『　』だ」を述べるときの根拠になります。

- **人が判断する場所**: ③の並び替えは「候補の提示」までで止め、**「気になる／見送る」を押すのは人**。アプリは点数を出すが、採用も却下もしない。そもそも①で「何を重視するか」を選ぶのも人。
- **その理由**: バイトを選ぶ基準（金か、人間関係か、シフトか）は人によって違い、平均点では決まらない。AI に足りないのは**「自分にとって何が大事か」の重みづけ**であり、それは①で人が選び、④で人が確定する。
